// Barrel export for lib/core/di — import this to get the service locator
// (`sl`), `setupLocator()`, and the GetIt package itself.
export 'package:get_it/get_it.dart';

export 'injection_container.dart';

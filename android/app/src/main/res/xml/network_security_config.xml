<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain http:// (cleartext) traffic by default on API 28+.
  This scopes an exception to local-dev-only hosts so the app can reach a
  backend running on your own machine before you've set up HTTPS for it —
  10.0.2.2 is the Android emulator's alias for the host machine's
  localhost. Everything else still requires https://.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
